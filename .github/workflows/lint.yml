<<<<<<< HEAD
﻿name: Lint
=======
﻿name: Lint & Tests
>>>>>>> d8071b3 (test(ci): add minimal smoke tests and enable pytest in CI)

on:
  push:
    branches: [ main, feature/*, chore/*, ci/*, test/* ]
  pull_request:
    branches: [ main ]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install flake8 black pytest

      - name: Run flake8 (only existing dirs)
        run: |
          set -e
          for d in core automation; do
            if [ -d "" ]; then flake8 --config .flake8 ""; fi
          done

      - name: Run black check (only existing dirs)
        run: |
          set -e
          paths=""
          for d in core automation; do
            if [ -d "" ]; then paths=" "; fi
          done
<<<<<<< HEAD
          if [ -n "" ]; then black --check ; else echo "No code dirs found"; fi
=======
          if [ -n "$paths" ]; then black --check $paths; else echo "No code dirs found"; fi

      - name: Set PYTHONPATH
        run: echo "PYTHONPATH=$GITHUB_WORKSPACE" >> $GITHUB_ENV

      - name: Run pytest (unit baseline)
        run: pytest -q
>>>>>>> d8071b3 (test(ci): add minimal smoke tests and enable pytest in CI)
